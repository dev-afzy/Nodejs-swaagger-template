const labelList = {
  serverError: 'Something went wrong',
  default: 'SUCCESS',
  invalidInput: 'Invalid input',
};

const reference = {
  category: ['children'],
};
module.exports = { labelList, reference };
